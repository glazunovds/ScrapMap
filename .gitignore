# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ScrapMap runtime data (never commit captures or locally extracted game art)
runtime/
captures/
tile-cache/
# Pristine copies of Scrap Mechanic's own scripts, kept only as a local restore
# baseline for tools/game-patch.mjs. They are Axolot's source, not ours, so they
# stay out of the repo; recreate with `snapshot` after verifying game files.
game-patch/vanilla/
public/map/runtime-data.local.js
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
