{
  "name": "scrapmap",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:hygiene": "node tools/check-repository-hygiene.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test-build.json && node --test tests/contracts.test.mjs tests/renderer-compat.test.mjs tests/overlay-profile-bridge.test.mjs tests/tile-atlas.test.mjs",
    "build": "pnpm run typecheck && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
